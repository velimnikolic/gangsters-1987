using UnityEngine;

namespace RoadDemo
{
    /// <summary>Marks geometry which must stay independent of ScenePerf's static chunks.</summary>
    [DisallowMultipleComponent]
    public sealed class StorefrontLive : MonoBehaviour { }
}
